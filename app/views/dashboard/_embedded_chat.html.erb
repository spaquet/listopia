<!-- app/views/dashboard/_embedded_chat.html.erb -->
<div class="bg-white rounded-xl shadow-sm border border-gray-200 flex flex-col" style="height: 600px;">
  <!-- Header - Minimal and Clean -->
  <div class="flex-shrink-0 border-b border-gray-200 px-6 py-4">
    <div class="flex items-center justify-end">
      <%= link_to "/chat/export", 
                  class: "p-2 text-gray-400 hover:text-gray-600 hover:bg-gray-100 rounded-lg transition-colors",
                  title: "Export chat" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
      <% end %>
    </div>
  </div>
  <!-- Messages Container -->
  <div id="dashboard-chat-messages" 
       class="flex-1 overflow-y-auto px-6 py-6 space-y-6"
       data-controller="dashboard-chat-scroll"
       data-dashboard-chat-scroll-target="container">
    <!-- This will be populated by load_dashboard_history or remain empty -->
  </div>
  <!-- Typing Indicator (hidden by default) -->
  <div id="dashboard-typing-indicator" class="hidden px-6 py-2">
    <div class="flex items-center gap-2 text-gray-500">
      <div class="flex gap-1">
        <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0s"></span>
        <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.15s"></span>
        <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.3s"></span>
      </div>
      <span class="text-sm">Thinking...</span>
    </div>
  </div>
  <!-- Input Area - Clean and Modern -->
  <div class="flex-shrink-0 border-t border-gray-200 px-6 py-4">
    <div data-controller="dashboard-chat-input" 
         data-dashboard-chat-input-context-value="<%= { page: 'dashboard#index' }.to_json %>">
      <div class="relative">
        <textarea 
          data-dashboard-chat-input-target="input"
          data-action="keydown->dashboard-chat-input#handleKeydown input->dashboard-chat-input#handleInput"
          placeholder="Message Listopia..."
          rows="1"
          class="w-full resize-none rounded-xl border border-gray-300 px-4 py-3 pr-12 focus:border-blue-500 focus:ring-2 focus:ring-blue-500 focus:ring-opacity-20 placeholder-gray-400 text-gray-900"
          style="max-height: 200px; min-height: 52px;"
        ></textarea>
        <button type="button"
                data-dashboard-chat-input-target="sendButton"
                data-action="click->dashboard-chat-input#sendMessage"
          disabled
          class="absolute right-2 bottom-2 p-2 rounded-lg bg-gray-100 text-gray-400 hover:bg-blue-600 hover:text-white disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:bg-gray-100 disabled:hover:text-gray-400 transition-all">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
          </svg>
        </button>
      </div>
      <!-- Help Text -->
      <div class="flex items-center justify-between mt-2 text-xs text-gray-500">
        <span>Press Enter to send, Shift+Enter for new line</span>
        <span data-dashboard-chat-input-target="charCount">0 / 2000</span>
      </div>
    </div>
  </div>
</div>