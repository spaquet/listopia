<%# app/views/connectors/connector_accounts/_account_card.html.erb %>
<div id="account_card_<%= account.id %>" class="connector-card">

  <!-- Quick Actions (icons only, on hover) -->
  <div class="connector-card-actions">
    <!-- Settings -->
    <%= link_to connectors_setting_path(connector_account_id: account.id),
              class: "connector-card-action",
              title: "Settings" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
      </svg>
    <% end %>

    <!-- Pause/Resume -->
    <% if account.active? %>
      <%= button_to pause_connectors_connector_account_path(account),
                    method: :patch,
                    class: "connector-card-action pause",
                    title: "Pause" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      <% end %>
    <% elsif account.paused? %>
      <%= button_to resume_connectors_connector_account_path(account),
                    method: :patch,
                    class: "connector-card-action resume",
                    title: "Resume" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"></path>
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      <% end %>
    <% end %>

    <!-- Disconnect -->
    <%= button_to connectors_connector_account_path(account),
                  method: :delete,
                  data: {
                    turbo_stream: true,
                    turbo_confirm: "Are you sure you want to disconnect this account?"
                  },
                  class: "connector-card-action delete",
                  title: "Disconnect" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    <% end %>
  </div>

  <!-- Status badge -->
  <span class="connector-card-status <%= account.status.downcase %>">
    <%= account.status.titleize %>
  </span>

  <!-- Provider and Account Info -->
  <div class="connector-card-info mb-3">
    <h3><%= account.provider.titleize %></h3>
    <p>
      <% if account.display_name.present? %>
        <%= account.display_name %>
      <% elsif account.email.present? %>
        <%= account.email %>
      <% else %>
        <%= account.provider_uid %>
      <% end %>
    </p>
  </div>

  <!-- Connected since -->
  <div class="connector-card-meta">
    Connected <%= time_ago_in_words(account.created_at) %> ago
  </div>
</div>
