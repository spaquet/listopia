<!-- app/views/admin/users/_results_summary.html.erb -->
<div class="flex items-center justify-between">
  <div>
    <p class="text-sm text-gray-600">
      Showing <span class="font-semibold text-gray-900"><%= users_count %></span>
      <span><%= "user".pluralize(users_count) %></span>
    </p>
  </div>
</div>