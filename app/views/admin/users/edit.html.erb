<!-- app/views/admin/users/edit.html.erb -->
<div class="max-w-2xl mx-auto space-y-6">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="t-display-m text-ink">Edit User</h1>
      <p class="text-ink-muted mt-1"><%= @user.email %></p>
    </div>
    <%= link_to admin_user_path(@user), class: "text-ink-muted hover:text-ink px-4 py-2 rounded-lg border border-rule hover:border-rule transition-colors duration-fast" do %>
      <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back
    <% end %>
  </div>
  <div class="bg-surface card border-rule p-6">
    <%= form_with(model: [:admin, @user], local: true, class: "space-y-6") do |form| %>
      <% if @user.errors.any? %>
        <div class="alert alert-danger">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-ink">
                <%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:
              </h3>
              <div class="mt-2 text-sm text-danger">
                <ul class="list-disc list-inside space-y-1">
                  <% @user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      <% end %>
      <!-- User Information Section -->
      <div>
        <h2 class="t-body-l font-semibold text-ink mb-4">User Information</h2>
        <div class="space-y-4">
          <!-- Name -->
          <div>
            <%= form.label :name, class: "block text-sm font-medium text-ink" %>
            <%= form.text_field :name, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "Full Name" %>
          </div>
          <!-- Email -->
          <div>
            <%= form.label :email, class: "block text-sm font-medium text-ink" %>
            <%= form.email_field :email, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "Email Address" %>
          </div>
          <!-- Bio -->
          <div>
            <%= form.label :bio, class: "block text-sm font-medium text-ink" %>
            <%= form.text_area :bio, 
                rows: 4,
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "User bio" %>
          </div>
          <!-- Avatar URL -->
          <div>
            <%= form.label :avatar_url, "Avatar URL", class: "block text-sm font-medium text-ink" %>
            <%= form.url_field :avatar_url, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "https://example.com/avatar.jpg" %>
          </div>
        </div>
      </div>
      <!-- Preferences Section -->
      <div>
        <h2 class="t-body-l font-semibold text-ink mb-4">Preferences</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <!-- Locale -->
          <div>
            <%= form.label :locale, class: "block text-sm font-medium text-ink" %>
            <%= form.select :locale, locale_options, {}, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent" %>
          </div>
          <!-- Timezone -->
          <div>
            <%= form.label :timezone, class: "block text-sm font-medium text-ink" %>
            <%= form.select :timezone, timezone_options, {}, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent" %>
          </div>
        </div>
      </div>
      <!-- Password Section (Optional) -->
      <div>
        <h2 class="t-body-l font-semibold text-ink mb-4">Password (Leave blank to keep current)</h2>
        <div class="space-y-4">
          <!-- Password -->
          <div>
            <%= form.label :password, class: "block text-sm font-medium text-ink" %>
            <%= form.password_field :password, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "New password" %>
          </div>
          <!-- Password Confirmation -->
          <div>
            <%= form.label :password_confirmation, class: "block text-sm font-medium text-ink" %>
            <%= form.password_field :password_confirmation, 
                class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
                placeholder: "Confirm password" %>
          </div>
        </div>
      </div>
      <!-- Admin Notes Section -->
      <div>
        <h2 class="t-body-l font-semibold text-ink mb-4">Admin Notes</h2>
        <div>
          <%= form.label :admin_notes, class: "block text-sm font-medium text-ink" %>
          <%= form.text_area :admin_notes, 
              rows: 3,
              class: "mt-1 block w-full rounded-lg border border-rule px-3 py-2 text-ink placeholder-gray-500 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-accent",
              placeholder: "Internal notes about this user" %>
        </div>
      </div>
      <!-- Form Actions -->
      <div class="flex items-center justify-between pt-4 border-t border-rule">
        <%= link_to admin_user_path(@user), class: "text-ink-muted hover:text-ink px-4 py-2 rounded-lg border border-rule hover:border-rule transition-colors duration-fast" do %>
          Cancel
        <% end %>
        <%= form.submit "Update User", class: "btn btn-primary transition-colors duration-fast font-medium" %>
      </div>
    <% end %>
  </div>
</div>