<div class="min-h-screen bg-surface-raised py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="t-display-m text-ink">Audit & Compliance Dashboard</h1>
      <p class="mt-2 text-ink-muted">Track changes, monitor compliance, and view audit trails</p>
    </div>

    <!-- Organization Context & Filter Options -->
    <div class="mb-6 bg-surface rounded-lg shadow p-4">
      <div class="flex items-center justify-between">
        <div>
          <p class="t-meta text-ink-muted">Organization</p>
          <p class="text-xl font-semibold text-ink"><%= @organization.name %></p>
        </div>

        <form method="get" class="flex gap-4 items-end">
          <div>
            <label class="block text-sm font-medium text-ink mb-1">Days to show</label>
            <select name="days" class="rounded border-rule shadow-sm px-3 py-2" onchange="this.form.submit()">
              <option value="7" <%= "selected" if @days == 7 %>>Last 7 days</option>
              <option value="30" <%= "selected" if @days == 30 %>>Last 30 days</option>
              <option value="90" <%= "selected" if @days == 90 %>>Last 90 days</option>
              <option value="365" <%= "selected" if @days == 365 %>>Last year</option>
            </select>
          </div>

          <div class="flex gap-3">
            <%= link_to compliance_report_admin_audit_index_path, class: "btn btn-primary inline-flex items-center gap-2" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              Compliance Report
            <% end %>
            <%= link_to activity_log_admin_audit_index_path, class: "btn btn-primary inline-flex items-center gap-2" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Activity Log
            <% end %>
            <%= link_to export_audit_admin_audit_index_path(format: :csv), class: "btn btn-primary inline-flex items-center gap-2" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
              </svg>
              Export
            <% end %>
          </div>
        </form>
      </div>
    </div>

    <% if @summary %>
      <!-- Summary Stats -->
      <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
        <div class="bg-surface rounded-lg shadow p-6">
          <div class="t-meta text-ink-muted">Total Events</div>
          <div class="t-display-m text-ink"><%= @summary[:total_events] %></div>
        </div>

        <div class="bg-surface rounded-lg shadow p-6">
          <div class="t-meta text-ink-muted">Event Types</div>
          <div class="t-display-m text-ink"><%= @summary[:events_by_type]&.keys&.count || 0 %></div>
        </div>

        <div class="bg-surface rounded-lg shadow p-6">
          <div class="t-meta text-ink-muted">Active Users</div>
          <div class="t-display-m text-ink"><%= @summary[:events_by_actor]&.keys&.count || 0 %></div>
        </div>

        <div class="bg-surface rounded-lg shadow p-6">
          <div class="t-meta text-ink-muted">Sensitive Changes</div>
          <div class="text-3xl font-bold text-danger"><%= @sensitive_changes.count %></div>
        </div>
      </div>

      <!-- Events by Type -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
        <div class="bg-surface rounded-lg shadow p-6">
          <h2 class="t-body-l font-semibold text-ink mb-4">Events by Type</h2>
          <div class="space-y-2">
            <% @summary[:events_by_type]&.each do |type, count| %>
              <div class="flex justify-between items-center">
                <span class="text-ink"><%= type %></span>
                <span class="t-body-l font-semibold text-ink"><%= count %></span>
              </div>
            <% end %>
          </div>
        </div>

        <div class="bg-surface rounded-lg shadow p-6">
          <h2 class="t-body-l font-semibold text-ink mb-4">Top Contributors</h2>
          <div class="space-y-2">
            <% @summary[:events_by_actor]&.to_a&.sort_by { |_, v| -v }&.first(5)&.each do |user, count| %>
              <div class="flex justify-between items-center">
                <span class="text-ink"><%= user %></span>
                <span class="t-body-l font-semibold text-ink"><%= count %></span>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Recent Changes -->
      <div class="bg-surface rounded-lg shadow overflow-hidden">
        <div class="px-6 py-4 border-b border-rule">
          <h2 class="t-body-l font-semibold text-ink">Recent Changes</h2>
        </div>
        <div class="overflow-x-auto">
          <table class="w-full">
            <thead class="bg-surface-raised border-b border-rule">
              <tr>
                <th class="px-6 py-3 text-left text-sm font-semibold text-ink">Timestamp</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-ink">User</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-ink">Event</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-ink">Details</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-rule">
              <% @recent_events.each do |event| %>
                <tr>
                  <td class="px-6 py-3 t-meta text-ink-muted"><%= event.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
                  <td class="px-6 py-3 text-sm text-ink"><%= event.actor&.email || "System" %></td>
                  <td class="px-6 py-3 text-sm text-ink"><%= event.event_type %></td>
                  <td class="px-6 py-3 t-meta text-ink-muted">
                    <% case event.event_type %>
                      <% when "list_item.created" %>
                        Created: <%= event.event_data["title"] %>
                      <% when "list_item.deleted" %>
                        Deleted: <%= event.event_data["title"] %>
                      <% when "list_item.assigned" %>
                        Assigned to user
                      <% when "list_item.status_changed" %>
                        <%= event.event_data["from"] %> → <%= event.event_data["to"] %>
                      <% else %>
                        <%= event.event_type %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    <% end %>
  </div>
</div>
