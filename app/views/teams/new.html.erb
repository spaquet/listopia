<div class="space-y-6 max-w-2xl">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="t-display-m text-ink">Create Team</h1>
      <p class="mt-2 text-ink-muted">Set up a new team in <%= @organization.name %></p>
    </div>
  </div>

  <div class="card">
    <%= render "form", organization: @organization, team: @team %>
  </div>
</div>
