<!-- app/views/lists/index.html.erb -->
<div class="space-y-6">
  <!-- Header -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
    <div>
      <h1 class="t-display-m text-ink">My Lists</h1>
      <p class="text-ink-muted mt-1 t-body">Organize your thoughts, tasks, and projects</p>
    </div>
    <div class="mt-4 sm:mt-0 flex gap-2">
      <%= link_to dashboard_items_kanban_path, class: "btn btn-secondary" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
        </svg>
        <span>Items Kanban</span>
      <% end %>
    </div>
  </div>
  <!-- Filters and Search -->
  <%= render "lists/filters" %>
  <!-- Lists Container - Proper structure for Turbo Stream updates -->
  <% if @lists.any? %>
    <div id="lists-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 auto-rows-fr">
      <% @lists.each do |list| %>
        <%= turbo_frame_tag "list_card_#{list.id}", class: "h-full" do %>
          <%= render "lists/list_card", list: list, current_user: current_user %>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <div id="lists-grid">
      <%= render "lists/empty_state" %>
    </div>
  <% end %>
</div>
<!-- Subscribe to real-time updates for this user's lists -->
<%= turbo_stream_from "user_lists_#{current_user.id}" %>